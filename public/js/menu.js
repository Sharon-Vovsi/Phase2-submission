/*open the board*/
function playGame() {
    const url = '/CardGame';
    window.open(url);
}

/*open login*/
function openLogin() {
    const url = '/login';
    window.open(url);
}

/*open register*/
function register() {
    const url = '/register';
    window.open(url);
}
/*--------------------------------------------------*/

